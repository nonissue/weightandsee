# Migration `20201024093446-update-person-plural`

This migration has been generated by nonissue at 10/24/2020, 3:34:46 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "people" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "created_at" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "name" TEXT NOT NULL,
    "nickName" TEXT
)

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_weighins" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "created_at" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updated_at" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "weighDate" DATETIME NOT NULL,
    "weight" INTEGER NOT NULL,
    "personId" INTEGER,

    FOREIGN KEY ("personId") REFERENCES "people"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_weighins" ("id", "created_at", "updated_at", "weighDate", "weight", "personId") SELECT "id", "created_at", "updated_at", "weighDate", "weight", "personId" FROM "weighins";
DROP TABLE "weighins";
ALTER TABLE "new_weighins" RENAME TO "weighins";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON

PRAGMA foreign_keys=off;
DROP TABLE "Person";
PRAGMA foreign_keys=on
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201024092106-init..20201024093446-update-person-plural
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -28,8 +28,10 @@
   updatedAt DateTime  @default(now()) @map(name: "updated_at")
   name      String
   nickName  String?
   weighIns  WeighIn[]
+
+  @@map(name: "people")
 }
 model WeighIn {
   id        Int      @id @default(autoincrement())
```


